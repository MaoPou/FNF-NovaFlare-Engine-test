# NovaFlare Engine Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Android Signing Configuration
ANDROID_KEYSTORE_PASSWORD=your-keystore-password-here
ANDROID_KEY_ALIAS=psychport
ANDROID_KEY_PASSWORD=your-key-password-here

# API Configuration
# Replace the hardcoded API key in OnlineStatistics.hx with this variable
API_KEY=your-api-key-here

# Encryption Configuration  
# Replace the hardcoded encryption key in LoginClient.hx with this variable
ENCRYPTION_KEY=your-32-character-hex-key-here

# Example for generating secure keys:
# - For API keys: Use a password manager or: openssl rand -hex 32
# - For encryption keys: openssl rand -hex 16 (for AES-128) or openssl rand -hex 32 (for AES-256)
